#include "jngen.h"
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
  registerGen(argc, argv);
  parseArgs(argc, argv);

  /*
     Trees
   */
  // Tree t = Tree::random(size);
  // t.printN();
  // t.printEdges();

  return 0;
}