#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n, q;
  cin >> n >> q;

  vector<int> p(n);
  for (int &i : p) cin >> i, i--;

  while (q--) {
    int l, r;
    cin >> l >> r;
    l--, r--;

  }

  return 0;
}