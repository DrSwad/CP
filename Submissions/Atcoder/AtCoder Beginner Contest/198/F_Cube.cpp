#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const int MOD = 998244353;

int main() {
  ll S;
  cin >> S;

  ll ans = 0;
  // 6 same
  if (S % 6 == 0) ans++;
  // 4 same, 2 diff

  return 0;
}