#include <bits/stdc++.h>
using namespace std;

void test_case() {}

pair<double, double> ar[205];

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n, k;
  scanf("%d %d", &n, &k);
  for (int i = 0; i < n; i++)
  {
    scanf("%lf %lf", &ar[i].first)
  }
  return 0;
}