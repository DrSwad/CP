#include <bits/stdc++.h>
using namespace std;

int main() {
  queue<int> q({1, 2, 3});
  stack<int> s({1, 2, 3});
  deque<int> d({1, 2, 3});
  debug(q.size());
  debug(s.size());
  debug(d.size());

  return 0;
}
